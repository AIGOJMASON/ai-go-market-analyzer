# CONTRACTOR BUILDER V1 — DELIVERY SHELL HANDOFF

## Status

INITIAL DELIVERY SHELL — READY FOR VALIDATION

This handoff records the first inheritance case for the frozen AI_GO child-core delivery template.

The child core is:

```text
contractor_builder_v1

It adopts the standard outward delivery pattern using:

unified system_view
canonical /operator route
stable outward sections
self-contained child-core service shell
initial probe ladder
Why This Exists

Market Analyzer V1 proved the delivery pattern once.

Contractor Builder V1 exists to prove that the pattern can be inherited by a second child core without inventing a new outward UX language.

This is the first transfer test of the frozen template.

Included Surfaces
1. Domain Registry

Declares the child-core identity and confirms adoption of the single-surface delivery model.

2. Request Schema

Provides bounded fixture and live request contracts.

3. Response Schema

Implements the standard unified outward object:

{
  "status": "...",
  "request_id": "...",
  "system_view": {
    "case": {},
    "runtime": {},
    "recommendation": {},
    "cognition": {},
    "pm_workflow": {},
    "governance": {}
  }
}
4. API Surface

Provides:

/contractor-builder/run
/contractor-builder/run/live
5. Operator UI

Provides canonical child-core operator route:

/operator
6. App Shell

Provides:

/
/healthz
/operator
child-core API routes
Domain Framing

This shell treats Contractor Builder V1 as an advisory planning surface for scoped contractor-style work.

The current shell models:

project type
trade focus
scope summary
budget band
timeline band
location mode
confirmation state

The recommendation surface currently emits planning-package style outputs, not execution actions.

Governance Posture

This shell preserves the same outward governance invariants as the frozen template:

advisory only
execution blocked
approval required
visible governance state
no mutation through outward delivery
Probe Coverage Included

This inheritance case includes the following probe set:

response probe
operator UI probe
API integration probe
app integration probe
golden-cases probe
operator route probe

This is the same delivery ladder pattern frozen for future child cores.

What Is Still Minimal

This shell is intentionally light.

It does not yet include:

deep contractor-domain logic
external feed integration
persistence
rich package generation
multi-step workflow tooling
long-horizon PM continuity storage

Its purpose is delivery inheritance first.

Next Step

Run the included probe ladder.

If the shell passes, Contractor Builder V1 becomes the first validated inheritance case of the AI_GO child-core delivery template.

At that point, domain logic can be expanded without reopening the delivery standard.

Final Note

This child core should be treated as a transfer test of the frozen delivery doctrine:

different domain, same governed outward delivery language