# INHERITANCE CONTRACT

allowed_upstream:
- PM_CORE

forbidden_upstream:
- direct RESEARCH_CORE
- external raw inputs

required_artifacts:
- refined_candidate_set
- upstream_receipt

receipt_required: true

target_core: {{CORE_ID}}