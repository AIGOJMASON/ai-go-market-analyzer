# TEMPLATE RULES

1. PM is the only allowed ingress authority
2. Raw research input is forbidden
3. All outputs must be structured artifacts
4. Watcher must verify all outputs
5. No execution allowed without approval gate
6. No narrative unless explicitly allowed
7. All flows must produce receipt trace
8. Constraints must be explicit and enforced